// Copy this file to "config.js" and paste your Unsplash access key below.
// config.js is gitignored, so your key never gets committed.
//
// Get a free key at https://unsplash.com/developers
//   1. Create an account / log in.
//   2. "Your apps" -> "New Application".
//   3. Accept the API terms, give it any name (e.g. "Cottagecore Lookbook").
//   4. Copy the "Access Key" (not the secret key) into the string below.
//
// If you leave this blank, the page still works — it falls back to
// https://source.unsplash.com (no photographer credit available in that mode).

window.UNSPLASH_ACCESS_KEY = "";
